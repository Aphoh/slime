apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-model-cache
  namespace: warnold-dynamo
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 36000Gi
  storageClassName: ""
  volumeMode: Filesystem
  volumeName: shared-model-cache-warnold-dynamo
